var miles = require ('../lib/miles')

describe('Miles', function () {
  it('our plan here', function () {  //anonymous callback function
    expect(miles.someFunction()).toEqual("running stuff for real")
  });
})
